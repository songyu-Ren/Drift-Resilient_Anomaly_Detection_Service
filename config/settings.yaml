artifacts_dir: "artifacts"
model_path: "artifacts/model.joblib"
metadata_path: "artifacts/metadata.json"

training:
  random_seed: 42
  n_samples: 1000
  n_anomalies: 50
  n_features: 3
  isolation_forest:
    n_estimators: 200
    contamination: 0.05
    random_state: 42

service:
  host: 0.0.0.0
  port: 8000